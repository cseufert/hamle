# Configuration file for unit test runner.
language: php
php:
  - 5.5
  - 5.6
  - 7.0
  - 7.1
  - 7.2
  - hhvm
matrix:
  allow_failures:
    - php: hhvm
before_script:
  - curl -sSf -o ~/.phpenv/versions/hhvm/bin/phpunit https://phar.phpunit.de/phpunit-5.7.25.phar
  - curl -sSf -o ~/.phpenv/versions/7.0/bin/phpunit https://phar.phpunit.de/phpunit-5.7.25.phar
  #before_script:
#  - cd test/php
script: phpunit
notifications:
  email: true
after_success:
    - bash <(curl -s https://codecov.io/bash)
sudo: false
